"use client";
import Link from "next/link";
import { useState } from "react";
export function AuthCard({mode}:{mode:"login"|"signup"}){const [magic,setMagic]=useState(false);const signup=mode==="signup";return <main className="auth-page"><Link href="/" className="brand"><span className="brand-mark">우</span><span>우리 둘의 공간</span></Link><section className="auth-card"><span className="auth-kicker">PRIVATE SPACE FOR TWO</span><h1>{signup?"우리의 공간을 만들어요":"다시 만나서 반가워요"}</h1><p>{signup?"작은 마음도 오래 남는 둘만의 기록을 시작해요.":"우리 둘의 오늘로 돌아갈 시간이에요."}</p>{magic?<div className="magic-sent"><b>✉ 로그인 링크를 보냈어요</b><p>받은 편지함을 확인해 주세요.</p><button onClick={()=>setMagic(false)}>다른 방법으로 로그인</button></div>:<form><label>이메일<input type="email" placeholder="name@example.com" required/></label><label>비밀번호<input type="password" placeholder="8자 이상 입력해 주세요" required/></label>{signup&&<label className="check"><input type="checkbox"/> 서비스 이용약관과 개인정보 처리방침에 동의해요</label>}<Link href={signup?"/onboarding":"/app"} className="button full">{signup?"계정 만들기":"로그인"}</Link><div className="or"><span/>또는<span/></div><button type="button" className="magic" onClick={()=>setMagic(true)}>✦ 이메일로 로그인 링크 받기</button></form>}<footer>{signup?"이미 공간이 있나요?":"처음 오셨나요?"} <Link href={signup?"/auth/login":"/auth/signup"}>{signup?"로그인":"가입하기"}</Link></footer></section><p className="secure-note">🔒 두 사람의 기록은 안전하게 보호돼요.</p></main>}
